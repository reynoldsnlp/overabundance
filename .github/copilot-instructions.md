All calls to python should use `uv run`.
This is a simple one-off research project, so it is more helpful to have extremely simple and easy-to-read code than it is to have highly optimized code with careful error handling and other production-quality features. Keep the code as simple as possible while still being correct.
